---
title: 预制体系统
type: knowledge
date: '2026-04-12'
status: distilled
tags:
- 知识
- 编程语言
- C#
- Unity基础
source: []
related:
- Unity游戏开发基础
- C# 编程语言学习笔记
- Transform和坐标系统
- 场景管理
---

# 预制体系统

## 预制体基础

### 创建与管理
```csharp
public class PrefabManager : MonoBehaviour
{
    public GameObject prefab;
    
    void CreatePrefabInstance()
    {
        // 实例化预制体
        GameObject instance = Instantiate(prefab);
        
        // 指定位置和旋转
        GameObject instance2 = Instantiate(
            prefab,
            new Vector3(0, 1, 0),
            Quaternion.identity
        );
        
        // 指定父物体
        GameObject instance3 = Instantiate(
            prefab,
            transform.position,
            transform.rotation,
            transform
        );
    }
    
    void DestroyPrefabInstance(GameObject instance)
    {
        // 立即销毁
        Destroy(instance);
        
        // 延迟销毁
        Destroy(instance, 2f);
    }
}
```

### 预制体变体
```csharp
public class PrefabVariant : MonoBehaviour
{
    // 不同变体的引用
    public GameObject basicPrefab;
    public GameObject enhancedPrefab;
    public GameObject elitePrefab;
    
    // 根据等级选择预制体
    public GameObject GetPrefabByLevel(int level)
    {
        if (level >= 10)
            return elitePrefab;
        else if (level >= 5)
            return enhancedPrefab;
        else
            return basicPrefab;
    }
}
```

## 预制体加载

### 资源加载
```csharp
public class PrefabLoader : MonoBehaviour
{
    // 从Resources加载
    void LoadFromResources()
    {
        GameObject prefab = Resources.Load<GameObject>("Prefabs/Enemy");
        if (prefab != null)
        {
            Instantiate(prefab);
        }
    }
    
    // 异步加载
    async Task<GameObject> LoadPrefabAsync(string path)
    {
        var request = Resources.LoadAsync<GameObject>(path);
        while (!request.isDone)
        {
            await Task.Yield();
        }
        return request.asset as GameObject;
    }
    
    // 批量加载
    IEnumerator LoadMultiplePrefabs(string[] paths)
    {
        foreach (string path in paths)
        {
            var request = Resources.LoadAsync<GameObject>(path);
            yield return request;
            
            if (request.asset != null)
            {
                Instantiate(request.asset as GameObject);
            }
        }
    }
}
```

### 对象池
```csharp
public class PrefabPool : MonoBehaviour
{
    [System.Serializable]
    public class Pool
    {
        public string tag;
        public GameObject prefab;
        public int size;
    }
    
    public List<Pool> pools;
    private Dictionary<string, Queue<GameObject>> poolDictionary;
    
    void Start()
    {
        poolDictionary = new Dictionary<string, Queue<GameObject>>();
        
        // 初始化对象池
        foreach (Pool pool in pools)
        {
            Queue<GameObject> objectPool = new Queue<GameObject>();
            
            for (int i = 0; i < pool.size; i++)
            {
                GameObject obj = Instantiate(pool.prefab);
                obj.SetActive(false);
                objectPool.Enqueue(obj);
            }
            
            poolDictionary.Add(pool.tag, objectPool);
        }
    }
    
    public GameObject SpawnFromPool(string tag, Vector3 position, 
        Quaternion rotation)
    {
        if (!poolDictionary.ContainsKey(tag))
        {
            Debug.LogWarning($"Pool with tag {tag} doesn't exist.");
            return null;
        }
        
        Queue<GameObject> pool = poolDictionary[tag];
        
        // 如果池为空，创建新对象
        if (pool.Count == 0)
        {
            Pool poolInfo = pools.Find(x => x.tag == tag);
            GameObject obj = Instantiate(poolInfo.prefab);
            pool.Enqueue(obj);
        }
        
        GameObject objectToSpawn = pool.Dequeue();
        objectToSpawn.SetActive(true);
        objectToSpawn.transform.position = position;
        objectToSpawn.transform.rotation = rotation;
        
        // 返回对象到池
        IPooledObject pooledObj = objectToSpawn.GetComponent<IPooledObject>();
        if (pooledObj != null)
        {
            pooledObj.OnObjectSpawn();
        }
        
        return objectToSpawn;
    }
    
    public void ReturnToPool(string tag, GameObject objectToReturn)
    {
        if (!poolDictionary.ContainsKey(tag))
        {
            Debug.LogWarning($"Pool with tag {tag} doesn't exist.");
            return;
        }
        
        objectToReturn.SetActive(false);
        poolDictionary[tag].Enqueue(objectToReturn);
    }
}

// 池化对象接口
public interface IPooledObject
{
    void OnObjectSpawn();
}
```

## 预制体修改

### 运行时修改
```csharp
public class PrefabModifier : MonoBehaviour
{
    public GameObject prefab;
    
    void ModifyPrefabInstance()
    {
        GameObject instance = Instantiate(prefab);
        
        // 添加组件
        instance.AddComponent<Rigidbody>();
        
        // 修改组件属性
        var renderer = instance.GetComponent<Renderer>();
        if (renderer != null)
        {
            renderer.material.color = Color.red;
        }
        
        // 修改Transform
        instance.transform.localScale *= 2f;
    }
    
    // 批量修改预制体实例
    void ModifyAllInstances()
    {
        GameObject[] instances = 
            GameObject.FindGameObjectsWithTag("PrefabInstance");
            
        foreach (var instance in instances)
        {
            // 应用修改
            ModifyInstance(instance);
        }
    }
    
    void ModifyInstance(GameObject instance)
    {
        // 实现具体的修改逻辑
    }
}
```

### 预制体链接
```csharp
public class PrefabLink : MonoBehaviour
{
    // 保持与预制体的链接
    public void UpdatePrefabLink(GameObject instance)
    {
        #if UNITY_EDITOR
        UnityEditor.PrefabUtility.RecordPrefabInstancePropertyModifications(
            instance);
        #endif
    }
    
    // 断开预制体链接
    public void BreakPrefabLink(GameObject instance)
    {
        #if UNITY_EDITOR
        UnityEditor.PrefabUtility.UnpackPrefabInstance(
            instance,
            UnityEditor.PrefabUnpackMode.Completely,
            UnityEditor.InteractionMode.AutomatedAction);
        #endif
    }
}
```

## 预制体优化

### 性能优化
```csharp
public class PrefabOptimization : MonoBehaviour
{
    // 预加载预制体
    void PreloadPrefabs(GameObject[] prefabs)
    {
        foreach (var prefab in prefabs)
        {
            // 预加载到内存
            var instance = Instantiate(prefab);
            instance.SetActive(false);
        }
    }
    
    // 延迟加载
    IEnumerator LoadPrefabsProgressively(GameObject[] prefabs)
    {
        foreach (var prefab in prefabs)
        {
            Instantiate(prefab);
            yield return new WaitForSeconds(0.1f);
        }
    }
    
    // 资源释放
    void CleanupPrefabs()
    {
        // 卸载未使用的资源
        Resources.UnloadUnusedAssets();
    }
}
```

### 预制体管理器
```csharp
public class PrefabRegistry : MonoBehaviour
{
    private static PrefabRegistry instance;
    public static PrefabRegistry Instance => instance;
    
    private Dictionary<string, GameObject> prefabCache = 
        new Dictionary<string, GameObject>();
    
    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
    
    // 注册预制体
    public void RegisterPrefab(string key, GameObject prefab)
    {
        if (!prefabCache.ContainsKey(key))
        {
            prefabCache.Add(key, prefab);
        }
    }
    
    // 获取预制体
    public GameObject GetPrefab(string key)
    {
        if (prefabCache.TryGetValue(key, out GameObject prefab))
        {
            return prefab;
        }
        return null;
    }
    
    // 清理缓存
    public void ClearCache()
    {
        prefabCache.Clear();
        Resources.UnloadUnusedAssets();
    }
}

## 相关笔记
<!-- AUTO-RELATED-START -->
- [Unity游戏开发基础](04_Unity基础.md)
- [C# 编程语言学习笔记](<../C#.md>)
- [Transform和坐标系统](03_Transform和坐标系统.md)
- [场景管理](05_场景管理.md)
<!-- AUTO-RELATED-END -->
