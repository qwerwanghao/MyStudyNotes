---
title: 动画系统
type: knowledge
date: '2026-04-12'
status: distilled
tags:
- 知识
- 编程语言
- C#
- Unity进阶
source: []
related:
- C# 编程语言学习笔记
- 游戏物理系统
- Unity音频系统
- Unity粒子系统
- Unity状态管理
---
# 动画系统

<!-- DEEP-TOPIC-GUIDE-START -->
> 动画系统不只是“让角色动起来”，而是决定表现层、状态层和交互层怎么同步。动画做得稳，角色表现和手感才会真正成立。

## 先读这篇时，建议带着这些问题

- Animation 和 Animator 分别适合什么情况
- 状态机、混合树、动画事件该如何配合
- IK 为什么常常是“表现自然度”的关键
- 动画为什么会和状态管理、物理系统、输入系统强耦合

## 推荐阅读顺序

1. Animation 组件
2. 动画事件
3. Animator 组件
4. 状态机控制
5. 混合树设置
6. IK
7. 和其他系统的协作

## 这一页最值得重点抓住的概念

- **动画系统本质上是状态驱动的表现系统**
- **动画事件是逻辑和表现的连接点**
- **混合树和状态机决定动作是否自然**
- **动画问题往往不是动画本身，而是和输入、物理、状态没对齐**

## 和其他笔记的关系

- 强相关： [游戏物理系统](01_游戏物理系统.md)
- 行为驱动： [Unity状态管理](../06_大型游戏开发实践/03_状态管理.md)
- 表现与反馈： [Unity音频系统](03_音频系统.md)
- 性能相关： [Unity性能优化](07_性能优化.md)
<!-- DEEP-TOPIC-GUIDE-END -->

## 横向关联主题
<!-- CROSS-LINKS-START -->
- [Unity音频系统](03_音频系统.md)：动作反馈通常要和声音同步才能形成完整体验
- [Unity粒子系统](04_粒子系统.md)：技能与动作表现常常依赖动画驱动特效触发
- [Unity状态管理](../06_大型游戏开发实践/03_状态管理.md)：动画状态机与角色逻辑状态通常要一起设计
<!-- CROSS-LINKS-END -->

## Animation组件

### 基础动画设置
```csharp
public class AnimationController : MonoBehaviour
{
    private Animation anim;
    
    void Start()
    {
        anim = GetComponent<Animation>();
        
        // 播放动画
        anim.Play("Idle");
        
        // 混合动画
        anim.Blend("Walk");
        
        // 交叉淡入动画
        anim.CrossFade("Run", 0.3f);
    }
    
    // 动画回调
    void OnAnimationComplete()
    {
        Debug.Log("Animation completed");
    }
    
    // 动画控制
    public void PlayAnimation(string animName, float fadeLength = 0.3f)
    {
        if (anim.IsPlaying(animName))
            return;
            
        anim.CrossFade(animName, fadeLength);
    }
}
```

### 动画事件
```csharp
public class AnimationEvents : MonoBehaviour
{
    // 在动画帧上调用的事件
    public void OnFootstep()
    {
        AudioManager.Instance.PlayFootstep();
    }
    
    public void OnAttackStart()
    {
        GetComponent<Weapon>().EnableDamage();
    }
    
    public void OnAttackEnd()
    {
        GetComponent<Weapon>().DisableDamage();
    }
    
    // 带参数的动画事件
    public void SpawnEffect(string effectName)
    {
        EffectManager.Instance.SpawnEffect(effectName, transform.position);
    }
}
```

## Animator组件

### 状态机控制
```csharp
public class AnimatorController : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        animator = GetComponent<Animator>();
        
        // 设置参数
        animator.SetBool("IsRunning", true);
        animator.SetFloat("Speed", 1.0f);
        animator.SetTrigger("Jump");
        
        // 获取参数
        bool isRunning = animator.GetBool("IsRunning");
        float speed = animator.GetFloat("Speed");
    }
    
    // 状态检查
    public bool IsInState(string stateName)
    {
        return animator.GetCurrentAnimatorStateInfo(0).IsName(stateName);
    }
    
    // 状态切换
    public void TransitionToState(string stateName)
    {
        animator.CrossFade(stateName, 0.25f);
    }
}
```

### 混合树设置
```csharp
public class BlendTreeController : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        animator = GetComponent<Animator>();
    }
    
    void Update()
    {
        // 设置混合参数
        float horizontal = Input.GetAxis("Horizontal");
        float vertical = Input.GetAxis("Vertical");
        
        // 更新混合树参数
        animator.SetFloat("X", horizontal);
        animator.SetFloat("Y", vertical);
        
        // 计算混合权重
        float speed = new Vector2(horizontal, vertical).magnitude;
        animator.SetFloat("Speed", speed);
    }
}
```

## 高级动画功能

### IK (Inverse Kinematics)
```csharp
public class IKController : MonoBehaviour
{
    private Animator animator;
    public Transform leftHandTarget;
    public Transform rightHandTarget;
    public Transform lookAtTarget;
    
    void Start()
    {
        animator = GetComponent<Animator>();
    }
    
    void OnAnimatorIK(int layerIndex)
    {
        if (animator)
        {
            // 设置视线IK
            if (lookAtTarget != null)
            {
                animator.SetLookAtWeight(1);
                animator.SetLookAtPosition(lookAtTarget.position);
            }
            
            // 设置手部IK
            if (leftHandTarget != null)
            {
                animator.SetIKPositionWeight(AvatarIKGoal.LeftHand, 1);
                animator.SetIKRotationWeight(AvatarIKGoal.LeftHand, 1);
                animator.SetIKPosition(AvatarIKGoal.LeftHand, 
                    leftHandTarget.position);
                animator.SetIKRotation(AvatarIKGoal.LeftHand, 
                    leftHandTarget.rotation);
            }
            
            if (rightHandTarget != null)
            {
                animator.SetIKPositionWeight(AvatarIKGoal.RightHand, 1);
                animator.SetIKRotationWeight(AvatarIKGoal.RightHand, 1);
                animator.SetIKPosition(AvatarIKGoal.RightHand, 
                    rightHandTarget.position);
                animator.SetIKRotation(AvatarIKGoal.RightHand, 
                    rightHandTarget.rotation);
            }
        }
    }
}
```

### 动画层管理
```csharp
public class AnimationLayerManager : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        animator = GetComponent<Animator>();
    }
    
    // 设置层权重
    public void SetLayerWeight(int layerIndex, float weight)
    {
        animator.SetLayerWeight(layerIndex, weight);
    }
    
    // 渐变层权重
    public IEnumerator FadeLayerWeight(int layerIndex, float targetWeight, 
        float duration)
    {
        float startWeight = animator.GetLayerWeight(layerIndex);
        float elapsed = 0f;
        
        while (elapsed < duration)
        {
            elapsed += Time.deltaTime;
            float weight = Mathf.Lerp(startWeight, targetWeight, 
                elapsed / duration);
            animator.SetLayerWeight(layerIndex, weight);
            yield return null;
        }
        
        animator.SetLayerWeight(layerIndex, targetWeight);
    }
}
```

## 动画优化

### 性能优化
```csharp
public class AnimationOptimization : MonoBehaviour
{
    private Animator animator;
    
    void Start()
    {
        animator = GetComponent<Animator>();
        
        // 设置Culling模式
        animator.cullingMode = AnimatorCullingMode.CullCompletely;
        
        // 优化更新模式
        animator.updateMode = AnimatorUpdateMode.Normal;
        
        // 关闭不需要的功能
        animator.applyRootMotion = false;
        
        // 设置动画质量
        QualitySettings.skinWeights = SkinWeights.TwoBones;
    }
    
    // 距离裁剪
    void Update()
    {
        float distanceToCamera = Vector3.Distance(
            transform.position, 
            Camera.main.transform.position
        );
        
        if (distanceToCamera > 50f)
        {
            animator.enabled = false;
        }
        else
        {
            animator.enabled = true;
        }
    }
}
```

### 动画复用
```csharp
public class AnimationReuse : MonoBehaviour
{
    // 动画覆盖控制器
    private AnimatorOverrideController overrideController;
    
    void Start()
    {
        var animator = GetComponent<Animator>();
        overrideController = new AnimatorOverrideController(
            animator.runtimeAnimatorController);
        animator.runtimeAnimatorController = overrideController;
    }
    
    // 替换动画片段
    public void OverrideAnimation(string animationName, 
        AnimationClip newClip)
    {
        overrideController[animationName] = newClip;
    }
    
    // 共享动画状态
    public void ShareAnimationState(Animator sourceAnimator)
    {
        var animator = GetComponent<Animator>();
        foreach (var parameter in sourceAnimator.parameters)
        {
            animator.SetFloat(parameter.name, 
                sourceAnimator.GetFloat(parameter.name));
        }
    }
}

## 相关笔记
<!-- AUTO-RELATED-START -->
- [Unity游戏开发进阶](<../C#.md>)
- [C# 编程语言学习笔记](<../C#.md>)
- [游戏物理系统](01_游戏物理系统.md)
- [Unity音频系统](03_音频系统.md)
<!-- AUTO-RELATED-END -->
