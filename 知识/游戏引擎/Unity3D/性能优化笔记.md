# 优化笔记

#### 一、优化界面卡顿

1. **读表格数据**：
   - 避免全表查询操作，使用索引列或单独解析Key。
   - 将服务器时间戳转换为配表格式，然后直接与表格里的时间戳比较，减少不必要的消耗。

2. **界面节点初始化**：
   - 将widget初始化逻辑放在`OnStartWidget`上，而非`OnLoad`（如列表创建、模型加载等）。

3. **界面节点过多的处理**：
   - **UIList优化**：
     - 使用`PMFlexibleWidgetPool`（分帧实例化） > `UIList`（实例化视口元素） > `PMWidgetPool`（实例化所有元素）。
   - **界面预载**：
     - 使用异步加载避免阻塞主线程，合理管理内存，避免预载过多不必要的界面。
     - 当预载的界面不再需要时，适时将其从场景中销毁，以释放内存。
   - **加转场动画**：
     - 使用转场动画可以在加载过程中提供视觉反馈，减少卡顿感。
   - **静态预制分离**：
     - 设计初期尽量将静态预制按功能划分，做好预制分离和动态加载。

4. **减少代码穿透调用**：
   - Lua访问C#需要将C#方法进行导出（`PMGameClientSrc\PMGame\Outputs\whitelist.xml`）。

5. **减少代码里不必要的消耗**：
   - 避免使用`DumpTable`（Lua）和`$`进行日志拼接（C#）。

6. **缓存机制**：
   - 通过缓存表，将信息预先存储在内存中，后续需要查询时，可以直接从内存中的缓存表读取。

7. **数据预处理**：
   - 使用辅助函数对原始数据进行一次性的预处理，转换为更容易操作的格式。

8. **列表刷新**：
   - 尽可能使用`Refresh`方法，而不是`SetCount`。

9. **变量初始化**：
   - 尽量在使用前初始化变量，避免大量直接用`nil`变量去判断（初始赋`false`值），避免触发`__index`机制，尤其是多继承。

10. **遍历优化**：
    - 尽量不要用`OrderedPairs`进行保序遍历。
    - 对于需要循环的内容，优先考虑`for`。在Lua中，`for`、`ipairs`和`pairs`的性能消耗差异通常不大，但数值`for`循环通常更快。

11. **协议优化**：
    - 直接使用Lua的协议，不要用C#协议。
    - 消息体过大时需要拆分，因Lua会自动填充proto的缺省值，会产生大量的动态内存分配。

12. **模型加载优化**：
    - 需要用到模型的界面尽量使用场景来代替RT，尤其是要显示两个模型以上的情景，RT会产生大量内存消耗。
    - 遇到需要大量变更模型的情景（如NX的快滑导航），做预备加载管理，在有当前正在加载中的模型时，随时替换下一个预备加载的信息，而不是直接拉起下一个模型的加载。

13. **逻辑简化**：
    - 对于需要多次查找的内容，可以做成`Dictionary`以降低过多循环造成的消耗。

    ---

#### 二、优化帧率

1. **减少界面顶点数量**：
   - 优化UI的顶点数量，减少复杂的UI元素和过多的顶点。

2. **减少Draw Call**：
   - 合理使用UI合批（Batching）技术，减少Draw Call数量。
   - 合并小的纹理图集，减少材质切换。

3. **优化动画**：
   - 对于复杂的动画，使用预烘焙动画或减少骨骼数量。
   - 使用动画压缩技术，减少动画数据的内存占用。

4. **减少物理计算**：
   - 对于不需要实时物理计算的对象，禁用物理组件（如`Rigidbody`）。
   - 使用简单的碰撞体代替复杂的碰撞体，减少物理计算开销。

5. **优化渲染**：
   - 使用LOD（Level of Detail）技术，根据距离动态调整模型的细节层次。
   - 对于远处的对象，使用低分辨率的模型和贴图。

6. **减少脚本开销**：
   - 避免在`Update`方法中执行复杂的逻辑，尽量将逻辑放在事件驱动的方法中。
   - 使用协程（Coroutine）来处理需要分帧执行的任务，避免一次性执行大量逻辑。

   ---

#### 三、优化内存

1. **资源管理**：
   - Lua中创建的资源及时`Destroy`。
   - Lua中引用了C#对象在销毁时需要主动解引用（界面绑定会自动解引用）。

2. **事件监听管理**：
   - 事件监听`Attach`和`Detach`一定要配对，避免内存泄漏。

3. **降低资源精度**：
   - 根据实际需求降低资源的精度，以减少内存占用。

4. **内存管理**：
   - 定期检查和清理不再使用的资源，避免内存泄漏。
   - 使用Unity的`Profiler`工具监控内存使用情况，找出内存泄漏和高内存占用的部分。

5. **对象池**：
   - 使用对象池技术来管理频繁创建和销毁的对象，减少GC压力，提高性能。

6. **网络优化**：
   - 减少网络数据包的大小，使用压缩技术。
   - 合理设计网络协议，减少不必要的数据传输。

7. **资源加载优化**：
   - 使用AssetBundle或Addressable来管理资源，按需加载，减少内存占用。
   - 对于大资源，使用分块加载技术，避免一次性加载过多资源。