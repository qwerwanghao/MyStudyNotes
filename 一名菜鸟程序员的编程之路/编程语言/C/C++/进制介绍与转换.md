# 学习笔记：进制介绍与转换

---

## 一、进制概述

**进制**（Numeral System）是表示数值的方式，基于不同的基数（Radix）。常见的进制包括二进制、十进制和十六进制等。

### 1. **二进制**（Base 2）

- **定义**：每位只能是 `0` 或 `1`。
- **位值**：`2^0, 2^1, 2^2, ...`
- **示例**：`1101` → `1*2^3 + 1*2^2 + 0*2^1 + 1*2^0 = 13`（十进制）

### 2. **十进制**（Base 10）

- **定义**：使用 `0-9` 表示。
- **位值**：`10^0, 10^1, 10^2, ...`
- **示例**：`2024` → `4*10^0 + 2*10^1 + 0*10^2 + 2*10^3`

### 3. **十六进制**（Base 16）

- **定义**：每位可以用 `0-9` 和 `A-F` 表示（其中 A-F 表示 10-15）。
- **位值**：`16^0, 16^1, 16^2, ...`
- **示例**：`ABC` → `10*16^2 + 11*16^1 + 12*16^0 = 2748`（十进制）

## 二、进制转换方法

### 1. **二进制 ↔ 十进制**

#### 二进制到十进制
- **方法**：将每位乘以 `2` 的幂次方，然后求和。
- **示例**：`1101` (二进制)  
  = `1*2^3 + 1*2^2 + 0*2^1 + 1*2^0`  
  = `13`（十进制）

#### 十进制到二进制
- **方法**：反复除以 `2`，取余数，直到商为 `0`，从下往上读取余数。
- **示例**：`13` (十进制)  
  `13 ÷ 2 = 6 余 1`  
  `6 ÷ 2 = 3 余 0`  
  `3 ÷ 2 = 1 余 1`  
  `1 ÷ 2 = 0 余 1`  
  → `1101` (二进制)

### 2. **二进制 ↔ 十六进制**

#### 二进制到十六进制
- **方法**：每 4 位分组，把每组转换为对应的十六进制数。
- **示例**：`11010011` (二进制)  
  → `1101 0011` → `D3` (十六进制)

#### 十六进制到二进制
- **方法**：将每个十六进制数字转换为 4 位二进制。
- **示例**：`D3` (十六进制)  
  → `1101 0011` (二进制)

### 3. **十进制 ↔ 十六进制**

#### 十进制到十六进制
- **方法**：反复除以 `16`，取余数，直到商为 `0`，从下往上读取余数。
- **示例**：`2748` (十进制)  
  `2748 ÷ 16 = 171 余 12 (C)`  
  `171 ÷ 16 = 10 余 11 (B)`  
  `10 ÷ 16 = 0 余 10 (A)`  
  → `ABC` (十六进制)

#### 十六进制到十进制
- **方法**：将每位乘以 `16` 的幂次方，然后求和。
- **示例**：`ABC` (十六进制)  
  = `10*16^2 + 11*16^1 + 12*16^0`  
  = `2748`（十进制）

## 三、进制转换的应用场景

### 1. **计算机内存与地址表示**
   - **二进制**：计算机内部使用二进制表示所有数据。
   - **十六进制**：由于二进制数过长，十六进制常用于表示内存地址和数据，便于阅读。

### 2. **网络通信**
   - **二进制与十六进制**：在数据传输时，协议中通常使用二进制数据，十六进制用于简洁表示和调试。

### 3. **图形与颜色编码**
   - **十六进制**：RGB颜色通常用十六进制表示，如 `#FF5733` 表示一种颜色。