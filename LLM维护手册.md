# LLM维护手册

## 目标

LLM 在这个仓库里的职责不是代替我思考，而是：

- 降低记录阻力
- 把碎片整理成结构化笔记
- 维护索引、链接和状态
- 帮我做周期性回顾

## 边界

### 允许做的事

- 根据 `收集/`、`日志/` 和已有笔记整理当天内容
- 新建或更新知识、感悟、总结、计划类笔记
- 合并明显重复的笔记
- 给笔记补充 frontmatter、标签、相关链接
- 把零散条目归档到更合适的位置

### 不允许做的事

- 擅自删除原始记录
- 把不确定的知识写成确定事实
- 把我的情绪、观点美化成并不存在的结论
- 大规模重命名或迁移现有目录，除非我明确要求

## 数据模型

### 笔记类型

- `capture`：原始碎片，通常放在 `收集/`
- `daily`：每日记录，通常放在 `日志/`
- `knowledge`：长期知识笔记，通常放在 `知识/`
- `insight`：个人判断、体会、方法论，通常放在 `感悟/`
- `review`：周、月、年、阶段复盘，通常放在 `总结/`
- `plan`：阶段规划、年度规划，通常放在 `展望/`

### Frontmatter

```yaml
---
title: 标题
type: knowledge
date: 2026-04-12
status: distilled
tags: []
source: []
related: []
---
```

状态约定：

- `raw`：原始记录或未整理内容
- `distilled`：已经梳理过，但还可能继续修改
- `evergreen`：长期稳定的知识笔记
- `active`：正在维护或完善的内容，保留已有用法，不自动等同于已核实。
- `archived`：已有归档材料；归档不代表失效或事实已核实，也不自动禁止用户要求的修订。

## 操作规则

### 新建还是更新

优先更新已有笔记。只有在下列情况下新建：

- 新主题和现有笔记边界明显不同
- 原笔记已经过长，不再适合继续扩展
- 需要把原始记录升级成长期笔记

### 命名建议

- 日志：`日志/YYYY/YYYY-MM-DD.md`
- 感悟：`MM.DD感悟.md` 或更清晰的主题名
- 总结：`2026年总结.md`、`2026-W15周回顾.md`
- 计划：`2026年计划.md`、`第二季度展望.md`
- 知识：尽量直接用主题名，不加“笔记”“总结”等空词

### 链接原则

- 长期笔记之间尽量互相链接
- 日志里只链接当天真正涉及的知识和感悟
- 索引页只放入口，不堆细节正文

### 知识目录约束

- `知识/` 下优先使用 `大类 / 主题 / 分组 / 文件` 结构
- 不再新增 `Content/`、`Media/` 这类中间目录
- 图片和示意图统一放在同级 `资源/`
- 当路径层级继续变深时，优先改文件名或补索引页，而不是继续套目录

### 遇到不确定内容时

- 标记 `待核实`
- 给出可能来源
- 不要强行补全

## 周期流程

### 每天

1. 读取 `收集/` 和当天 `日志/`
2. 更新今日日志
3. 把明显属于长期知识的内容整理到 `知识/`
4. 把明显属于个人判断的内容整理到 `感悟/`
5. 把明显属于行动和阶段方向的内容整理到 `展望/`
6. 如有必要，更新 [索引/工作台.md](索引/工作台.md)

### 每周

1. 汇总最近 7 天日志
2. 生成周回顾草稿到 `总结/`
3. 找出重复出现的问题、主题、目标
4. 标记值得长期沉淀的主题

### 每月 / 每年

1. 基于日志和周回顾生成阶段总结
2. 把阶段总结和未完成事项转成下一阶段计划
3. 保留原始材料，不覆盖旧判断

## 调用方式

- 单次执行 prompt 统一放在 `提示词/`
- `提示词/` 只负责一次任务怎么执行
- 本文件负责长期维护约定；用户当前明确要求决定本次范围。AGENTS.md 和 CLAUDE.md 作为入口引用本文件，避免重复维护相互冲突的规则。


## 属性兼容规则

上面的 YAML 是可复制的单一取值示例，不要把 `raw | distilled` 等选项说明整体写进属性值。

- 结构化记录使用 `title`、`type`、`date`、`status`、`tags`、`source`、`related`。导航、README、上下文说明、提示词和简历不强制添加 frontmatter。
- `tags`、`source`、`related` 使用一层字符串列表。空值用 `[]`，不要嵌套列表或对象。
- 属性内双链必须加引号。例如：`related: ["[[知识]]"]`。`[[知识]]` 指向文件名，不是任意 title 属性或别名。
- `related` 中已有纯文本名称保留为描述；只有确认真实文件目标时才转换为双链，不能把纯文本当作已验证的链接。
- `source` 可存 URL、已确认文件链接或来源说明。复杂归档来源的类型、标题、说明保留在正文“来源详情”中，避免属性面板不支持嵌套对象。
- 标签不用空格或 `#`；例如 `Claude-Code`、`CSharp`。修改标签不需要修改正文中的技术名称。
- 记录的 `date` 保留其原始语义，不因格式整理改成今天。未知日期不要猜，可留空并说明待核实。
- 模板中的 `YYYY-MM-DD` 是待填写占位符，正式记录必须替换为实际日期。模板本身不按正式记录的日期有效性判断。
- `active`、`archived` 是已有工作状态，不能仅为统一格式擅自升级成 `distilled` 或 `evergreen`。

## 执行范围与重复整理

- 开始时明确目标日期或起止日期，默认使用用户当前时区。不能仅凭文件修改时间推断事件发生日期。
- 今日素材不足时如实报告；不要把旧 Inbox 全部重新当作今日输入，也不要凑出固定数量的“成果”。
- 已整理内容通过日志中的来源、整理记录或目标笔记链接识别。再次执行先比较已有内容，只补充增量，不重复追加。
- 整理知识、感悟、计划时保留来源线索；无法确认是否已完成的任务保留原状态。
- “建议行动”和“用户已决定的行动”明确区分，不把 AI 建议写成用户承诺。
- 周回顾默认最近 7 个自然日，包含目标日；文件名采用 `总结/YYYY/YYYY-MM-DD周回顾.md`，在正文写清起止日期。按 ISO 周整理时才使用 `YYYY-Www周回顾.md` 并核对跨年周。
- 月总结使用 `总结/YYYY/YYYY-MM月总结.md`，年度总结使用 `总结/YYYY/YYYY年总结.md`；已有目标文件优先更新。
- 输入不足的日期不补造日志；周期草稿标明实际覆盖范围与缺失材料。
- 文档中写有每日、每周节奏不等于自动运行。创建定时任务、提交和推送需有对应用户授权。

## 使用方式与应用设置

- AI 直接读写 Markdown 不依赖 Obsidian 模板插件，也不要求记住 CLI 命令。
- 当前模板是 AI 或手工填写的骨架，普通 `YYYY-MM-DD` 不会由 Obsidian 自动替换。若以后配置核心模板插件，需同时使用官方 `{{date}}` 等变量并验证生成结果；不要只改说明却声称自动化已生效。
- 使用核心日记插件前，确认输出路径为 `日志/YYYY/YYYY-MM-DD.md`，否则可能在根目录创建日记。未配置前沿用文件方式创建。
- 本机已验证可通过 `C:\Program Files\Obsidian\Obsidian.com` 调用 CLI；其他设备先确认安装位置和 CLI 是否启用，不照搬本机绝对路径。
- 验证至少包含属性、代码围栏、链接目标和 Obsidian 预览；外链可达性与技术事实是单独的审查范围。

官方参考：[属性格式](https://help.obsidian.md/properties)、[标签规则](https://help.obsidian.md/tags)、[模板变量](https://help.obsidian.md/plugins/templates)。
